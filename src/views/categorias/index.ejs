<%- include('../partials/header', { title }) %>

<div class="container-fluid">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="bi bi-tags"></i> Categorías</h1>

    <div class="d-flex gap-2 align-items-center">
      <!-- Botones de vista (solo en desktop) -->
      <div class="btn-group d-none d-lg-flex" role="group">
        <button type="button" class="btn btn-outline-secondary btn-sm active" id="viewTable" title="Vista tabla">
          <i class="bi bi-list-ul"></i>
        </button>
        <button type="button" class="btn btn-outline-secondary btn-sm" id="viewCards" title="Vista tarjetas">
          <i class="bi bi-grid-3x2-gap"></i>
        </button>
      </div>
      
      <!-- Botón nueva categoría -->
      <a href="/categorias/nuevo" class="btn btn-success">
        <i class="bi bi-plus-circle me-1"></i> Nueva Categoría
      </a>
    </div>
  </div>

  <!-- Vista de TABLA (desktop) -->
  <div class="d-none d-lg-block" id="tableView">
    <div class="card shadow-sm">
      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
              <tr>
                <th class="ps-4">#</th>
                <th>Nombre</th>
                <th>Icono</th>
                <th>Vista Previa</th>
                <th class="text-end pe-4">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% categorias.forEach((cat, index) => { %>
                <tr>
                  <td class="ps-4">
                    <span class="badge bg-secondary"><%= index + 1 %></span>
                  </td>
                  <td>
                    <div class="d-flex align-items-center">
                      <i class="<%= cat.icono || 'bi bi-tag' %> me-2 text-primary" 
                         style="font-size: 1.2rem;"></i>
                      <span class="fw-semibold"><%= cat.nombre %></span>
                    </div>
                  </td>
                  <td>
                    <div class="d-flex align-items-center">
                      <i class="<%= cat.icono || 'bi bi-tag' %> me-2" 
                         style="font-size: 1.5rem;"></i>
                      <code class="small text-muted"><%= cat.icono || 'bi bi-tag' %></code>
                    </div>
                  </td>
                  <td>
                    <span class="badge bg-primary" style="padding: 8px 12px;">
                      <i class="<%= cat.icono || 'bi bi-tag' %> me-1"></i>
                      <%= cat.nombre %>
                    </span>
                  </td>
                  <td class="text-end pe-4">
                    <div class="btn-group" role="group">
                      <a href="/categorias/<%= cat.id %>/editar" 
                         class="btn btn-sm btn-outline-primary" 
                         title="Editar">
                        <i class="bi bi-pencil"></i>
                      </a>
                      <form action="/categorias/<%= cat.id %>/eliminar" method="post" style="display:inline;">
                        <button class="btn btn-sm btn-outline-danger" 
                                onclick="return confirm('¿Estás seguro de eliminar esta categoría?')" 
                                title="Eliminar">
                          <i class="bi bi-trash"></i>
                        </button>
                      </form>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- Vista de CARDS -->
  <div class="d-none" id="cardsView">
    <div class="row g-3">
      <% categorias.forEach((cat, index) => { %>
        <div class="col-12 col-md-6 col-lg-4">
          <div class="card shadow-sm border-0 h-100">
            <div class="card-body">
              <!-- Header de la tarjeta -->
              <div class="d-flex align-items-start mb-3">
                <div class="icon-wrapper p-3 rounded-3 me-3 bg-primary bg-opacity-10">
                  <i class="<%= cat.icono || 'bi bi-tag' %> text-white" 
                     style="font-size: 2rem;"></i>
                </div>
                <div class="flex-grow-1">
                  <h5 class="mb-1"><%= cat.nombre %></h5>
                  <span class="badge bg-secondary">#<%= index + 1 %></span>
                </div>
              </div>

              <!-- Detalles -->
              <div class="mb-3">
                <div class="d-flex align-items-center">
                  <span class="text-muted me-2">Icono:</span>
                  <code class="small"><%= cat.icono || 'bi bi-tag' %></code>
                </div>
              </div>

              <!-- Vista previa -->
              <div class="p-3 bg-light rounded-3 mb-3 text-center">
                <span class="badge bg-primary" style="padding: 10px 16px; font-size: 0.9rem;">
                  <i class="<%= cat.icono || 'bi bi-tag' %> me-1"></i>
                  <%= cat.nombre %>
                </span>
              </div>

              <!-- Acciones -->
              <div class="d-flex gap-2">
                <a href="/categorias/<%= cat.id %>/editar" class="btn btn-sm btn-primary flex-fill">
                  <i class="bi bi-pencil"></i> Editar
                </a>
                <form action="/categorias/<%= cat.id %>/eliminar" method="post" class="flex-fill">
                  <button class="btn btn-sm btn-danger w-100" 
                          onclick="return confirm('¿Estás seguro de eliminar esta categoría?')">
                    <i class="bi bi-trash"></i> Eliminar
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <!-- Mensaje si no hay categorías -->
  <% if (categorias.length === 0) { %>
    <div class="text-center py-5">
      <i class="bi bi-tags" style="font-size: 4rem; color: #dee2e6;"></i>
      <p class="text-muted mt-3">
        No hay categorías registradas aún
      </p>
      <a href="/categorias/nuevo" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Agregar primera categoría
      </a>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>