<%- include('../partials/header', { title }) %>

<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-6">
      <div class="card shadow-sm">
        <div class="card-header bg-white py-3">
          <h4 class="mb-0">
            <i class="bi bi-exclamation-triangle me-2"></i>
            <%= title %>
          </h4>
        </div>
        <div class="card-body">
          <form action="<%= action %>" method="post">
            <!-- Campo Nombre -->
            <div class="mb-4">
              <label class="form-label fw-semibold">Nombre</label>
              <div class="input-group">
                <span class="input-group-text bg-light">
                  <i class="bi bi-exclamation-circle-fill"></i>
                </span>
                <input type="text" 
                       name="nombre" 
                       class="form-control" 
                       value="<%= urgencia.nombre || '' %>" 
                       placeholder="Ej: Alta, Media, Baja"
                       required>
              </div>
            </div>

            <!-- Campo Color -->
            <div class="mb-4">
              <label class="form-label fw-semibold">Color</label>
              <div class="d-flex align-items-center gap-3">
                <input type="color" 
                       name="color" 
                       class="form-control form-control-color" 
                       value="<%= urgencia.color || '#6c757d' %>"
                       style="width: 60px; height: 40px;">
                <span class="text-muted">Selecciona un color para identificar esta urgencia</span>
              </div>
            </div>

            <!-- Selector de Iconos -->
            <div class="mb-4">
              <label class="form-label fw-semibold">Icono</label>
              <input type="hidden" name="icono" id="iconoInput" value="<%= urgencia.icono || 'bi bi-exclamation-circle-fill' %>">
              
              <div class="border rounded p-3 bg-light">
                <div class="row g-2">
                  <%
                    const iconos = [
                      'bi bi-exclamation-circle-fill', 'bi bi-exclamation-circle', 'bi bi-exclamation-triangle-fill',
                      'bi bi-exclamation-triangle', 'bi bi-lightning', 'bi bi-lightning-charge', 'bi bi-bell-fill',
                      'bi bi-bell', 'bi bi-alarm-fill', 'bi bi-alarm', 'bi bi-stopwatch', 'bi bi-stopwatch-fill'
                    ];
                    iconos.forEach(icono => { %>
                      <div class="col-3 col-md-2 text-center">
                        <button type="button"
                          class="btn btn-outline-secondary w-100 p-3 icono-btn <%= (urgencia.icono === icono || (!urgencia.icono && icono === 'bi bi-exclamation-circle-fill')) ? 'active' : '' %>"
                          data-icono="<%= icono %>">
                          <i class="<%= icono %>" style="font-size: 1.5rem;"></i>
                        </button>
                      </div>
                  <% }) %>
                </div>
              </div>
            </div>

            <!-- Vista previa -->
            <div class="mb-4">
              <label class="form-label fw-semibold">Vista previa</label>
              <div class="p-4 bg-light rounded text-center">
                <span class="badge" 
                      id="previewBadge"
                      style="background-color: <%= urgencia.color || '#6c757d' %>; 
                             padding: 12px 24px; 
                             font-size: 1rem;">
                  <i id="iconoPreview" 
                     class="<%= urgencia.icono || 'bi bi-exclamation-circle-fill' %> me-2"></i>
                  <span id="nombrePreview"><%= urgencia.nombre || 'Nombre de urgencia' %></span>
                </span>
              </div>
            </div>

            <!-- Botones -->
            <div class="d-flex gap-2 justify-content-end">
              <a href="/urgencias" class="btn btn-secondary">
                <i class="bi bi-x-circle me-1"></i> Cancelar
              </a>
              <button type="submit" class="btn btn-primary">
                <i class="bi bi-check-circle me-1"></i> Guardar
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>